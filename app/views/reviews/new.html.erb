<h1>Review</h1>

<%= simple_form_for [@spaceship, @review] do |f| %>
  <%= f.input :description %>
  <%= f.input :rating %>
  <%= f.submit %>
  <% end %>
<%= link_to 'Back', spaceship_path(@spaceship) %>